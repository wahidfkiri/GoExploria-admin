<section id="hebergement">
  <div class="container"><div class="section-eyebrow">Hébergement</div><h2 class="section-title">Hôtels <span class="italic">& Partenaires</span></h2><p class="section-sub">Des établissements soigneusement sélectionnés pour un confort optimal à chaque étape.</p><div class="hotels-grid">@foreach($hotelCards as $hotel)<article class="hotel-card"><div class="hotel-img"><img src="{{ $hotel['image'] }}" alt="{{ $hotel['name'] }}"><div class="hotel-stars">@for($i=0;$i<$hotel['stars'];$i++)★@endfor</div></div><div class="hotel-body"><div class="hotel-name">{{ $hotel['name'] }}</div><div class="hotel-city">📍 {{ $hotel['city'] }}</div><div class="hotel-amenities">@foreach($hotel['amenities'] as $amenity)<span class="amenity">{{ $amenity }}</span>@endforeach</div></div></article>@endforeach</div><div style="text-align:center;margin-top:36px"><a href="#contact" class="btn btn-outline" style="padding:14px 28px">Demander les options d’hébergement →</a></div></div>
</section>
